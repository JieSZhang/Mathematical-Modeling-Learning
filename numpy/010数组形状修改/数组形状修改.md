##获取数组的形状
可通过arr.shape得到数组的形状。
结果会返回一个元组，元组的索引数代表维度数，对应的整数值表示该维度有几个元素。

##修改数组形状
可通过arr.reshape()修改形状，但修改的时候需要注意必须保持元素总数不变，不然会报错。

##未知的维
当我们将一个已知数组转化为n维数组时，已经设置了n-1个元素数，最后一个元素数可以用-1表示，代码会自动调整好最后一个元素数，得到对应的数组。

##展平数组
通过arr.reshape(-1)的方法将多维数组转化为一维数组