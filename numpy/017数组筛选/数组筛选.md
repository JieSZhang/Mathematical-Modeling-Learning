##数组筛选

##数组过滤
从现有数组中取出一些元素并从中创建新数组成为过滤。
在NumPy中，我们使用布尔索引列表来过滤数组。
布尔索引列表是与数组中的索引相对应的布尔值列表。
如果索引处的值为True，则该元素包含在过滤后的数组中；如果索引处的值为False，则该元素将过滤后的数组中派出。

##创建过滤器数组
我们可以通过if语句，创建一个过滤数组。

##直接从数组创建过滤器
我们可以在条件中直接替换数组而不是 iterable 变量，它会如我们期望地那样工作。